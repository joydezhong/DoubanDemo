var express = require('express');
var router = express.Router();

// var mongoose = require('mongoose');
// var TopMovie = require('../models/topMovieModel');

router.get('/',function (req, res, next) {
    console.log(res);
});


module.exports = router;
